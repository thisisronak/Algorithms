#include <bits/stdc++.h>
using namespace std;
#ifdef CORLEONE
  #define d(b) cerr<< #b << " " << b << endl
  #else
  #define d(b)
#endif

vector <int> G[100005];

int main()
{
  //ios::sync_with_stdio(false);cin.tie(0);
  int n;
  cin>>n;
  for(int t =0;t<n;t++){
    int a, b, f;
    cin >> a>> b>>t;
    
  }
}