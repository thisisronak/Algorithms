#include <bits/stdc++.h>
using namespace std;
#define d(n) cout<<#n<<" "<<n<<endl

int main()
{
  // freopen("input.txt","r",stdin);
  int w;
  cin>>w;
  if(w==2) cout<<"NO"<<endl;
  else if(w%2==0) cout<<"YES";
  else cout<<"NO"<<endl; 
}