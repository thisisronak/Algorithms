#include <bits/stdc++.h>
using namespace std;
#ifdef CORLEONE
  #define d(b) cerr<< #b << " " << b << endl
  #else
  #define d(b)
#endif

int dp[305];

void thisIsACall(){

  int count =0;
  int ten = 0;
  for(int i=0;i<=300;i++){
    if(count%10==0)
    {
      ten = count;
    }
  }

}

int main()
{
  //ios::sync_with_stdio(false);cin.tie(0);
  
}