#include <bits/stdc++.h>
using namespace std;
//ios_base::sync_with_stdio(false);
#ifdef CORLEONE
  #define d(b) cerr<< #b << " " << b << endl
  #else
  #define d(b)
#endif
#define limit 10000009

//we were born and raised in a summer haze;
int dp[1110][105];
int t,d;
void fill(int s, int d){
  for(int )
}
int main()
{
  int init, fin;
  cin>>init>>fin>>t>>d;
  
  dp[0][init-1] = init;
  for(int i=1;i<t;i++){
    
  }

}