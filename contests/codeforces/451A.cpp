#include <bits/stdc++.h>
using namespace std;

#ifdef CORLEONE
  #define d(b) cerr<< #b << " " << b << endl
  #else
  #define d(b)
#endif

int main()
{
  int a , b;
  cin>>a>>b;

  if( a%2 == 0 && b%2==0) cout<<"Malvika"<<endl;
  else if( a%2!=0 && b%2!==0) cout<<"Akshat"<<endl;

  else
  {
    if ()
  } 
}