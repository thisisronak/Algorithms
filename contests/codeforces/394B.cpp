#include <bits/stdc++.h>
using namespace std;
//ios_base::sync_with_stdio(false);
#ifdef CORLEONE
  #define d(b) cerr<< #b << " " << b << endl
  #else
  #define d(b)
#endif

int main()
{
  string p, x;
  cin>>p>>x;
  long long
  int s,a,b, l = p.size();
  if( (l-1-x)/(10*x-1) == 0 )
    a = (l-1-x)/(10*x-1);
  else {
    cout<<"Impossible"<<endl;
    return 0;
  }
  while(a<)
  cout<<a<<endl;

}