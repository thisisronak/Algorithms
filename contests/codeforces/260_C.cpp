#include<bits/stdc++.h>
using namespace std;
long long n;
long long arr[100001];
long long dp[100001];
long long func(long long ind)
{
    if(dp[ind]!=-1)
}

int main()
{
    memset (dp,-1,sizeof(dp));
    cin>>n;
    for(long long i=0;i<n;i++ )
    {
        long long x;
        cin>>x;
        arr[x]++;
    }
    cout<<func(100000)<<endl;

}
