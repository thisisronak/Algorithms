  #include <bits/stdc++.h>
using namespace std;
//ios_base::sync_with_stdio(false);
#ifdef CORLEONE
  #define d(b) cerr<< #b << " " << b << endl
  #else
  #define d(b)
#endif

int main()
{
  int n;
  string s;
  cin>>n>>s;
  for(int i=0;i<s.size();i=i+2){
    if()
    cout<<s[i]<<s[i+1];
    if(i!=s.size()-2) cout<<"-";
  } 
}