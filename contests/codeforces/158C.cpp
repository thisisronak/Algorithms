#include <bits/stdc++.h>
using namespace std;
#ifdef CORLEONE
  #define d(b) cerr<< #b << " " << b << endl
  #else
  #define d(b)
#endif
 string add = "/";
string con;
void cd( ){
  add = add
}
int main()
{
  //ios::sync_with_stdio(false);cin.tie(0);
  int n;
  cin>>n;
  // string add = '/';
  for(int i=0;i<n;i++){
    string pass;
    cin>>pass;
    if(pass= "pwd")
    {
      cout<<add<<endl;
    }
    else{
      
    }

  } 
}

