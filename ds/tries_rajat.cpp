#include <bits/stdc++.h>

using namespace std;

const int N = 100010;
int trie[N][26];
bool mark[N][26];


int main() {
  while(true) {
    char ch;
    cin >> ch;
    string s;
    cin >> s;
    if(ch == 'i') {
      int sz = s.size();
      for(int i = 0; i < sz; i++) {
        int x = s[i] - 'a';
        if(trie[])
      }
    }
    else {

    }
  }
}